---
import { env } from 'cloudflare:workers';
import Layout from "@layouts/Layout.astro";
import { getArticles } from "@modules/articles";
import { getCategories } from "@modules/categories";
import { getTags } from "@modules/tags";
import {
  hydrateArticles,
  hydrateCategories,
  hydrateTags,
} from "@shared/utils/hydration";
import { extractImage, getImageSrcSet } from "@shared/utils";
import { parseCachedRecipe } from "@modules/articles/utils/cached-fields";
import Pagination from "@components/Pagination.astro";
import {
  buildRecipeListingUrl,
  normalizeRecipeSearch,
} from "@site/utils/recipe-listing";

export const prerender = false;

// Get filter params from URL
const url = Astro.url;
const categoryFilter = url.searchParams.get("category") || "";
const tagFilter = url.searchParams.get("tag") || "";
const searchTerm = normalizeRecipeSearch(url.searchParams.get("search"));
const page = parseInt(url.searchParams.get("page") || "1");
const limit = 12;
const offset = (page - 1) * limit;

// Fetch data - use any[] to avoid complex type inference issues with hydration
let recipes: any[] = [];
let totalRecipes = 0;
let categories: any[] = [];
let tags: any[] = [];

try {
  if (env.DB) {
    // Get articles with optional filter
    const result = await getArticles(env.DB, {
      type: "recipe",
      categorySlug: categoryFilter || undefined,
      tagSlug: tagFilter || undefined,
      search: searchTerm || undefined,
      limit,
      offset,
    });
    recipes = result.items;
    totalRecipes = result.total;

    // Get all categories and tags for filters
    const rawCategories = await getCategories(env.DB, { workflow_status: 'published' });
    const rawTags = await getTags(env.DB);
    categories = hydrateCategories(rawCategories);
    tags = hydrateTags(rawTags);
  }
} catch (error) {
  console.error("Error fetching recipes:", error);
}

const totalPages = Math.ceil(totalRecipes / limit);

const getRecipeImage = (
  recipe: any,
  targetWidth: number,
  prefer: "hero" | "thumbnail" = "hero"
) => {
  const hero = extractImage(recipe.images_json, "hero", targetWidth);
  const thumbnail = extractImage(recipe.images_json, "thumbnail", targetWidth);
  const slotName =
    prefer === "hero"
      ? hero.image_url
        ? "hero"
        : "thumbnail"
      : thumbnail.image_url
        ? "thumbnail"
        : "hero";
  const selected = slotName === "hero" ? hero : thumbnail;
  const srcSet = getImageSrcSet(recipe.images_json, slotName);
  return { selected, srcSet };
};

const parseHexColor = (value?: string) => {
  if (!value) return null;
  const trimmed = value.trim();
  const match = trimmed.startsWith("#") ? trimmed.slice(1) : trimmed;
  if (!/^[0-9a-fA-F]+$/.test(match)) return null;
  if (match.length === 3 || match.length === 4) {
    const [r, g, b] = match.slice(0, 3).split("");
    return `${r}${r}${g}${g}${b}${b}`;
  }
  if (match.length === 6 || match.length === 8) {
    return match.slice(0, 6);
  }
  return null;
};

const getBadgeTextColor = (background?: string) => {
  const hex = parseHexColor(background);
  if (!hex) return "var(--text-inverse)";
  const r = parseInt(hex.slice(0, 2), 16);
  const g = parseInt(hex.slice(2, 4), 16);
  const b = parseInt(hex.slice(4, 6), 16);
  const yiq = (r * 299 + g * 587 + b * 114) / 1000;
  return yiq >= 170 ? "var(--text-dark, #111111)" : "var(--text-inverse, #ffffff)";
};

const getBadgeColors = (color?: string) => {
  const badgeColor = color && color.trim() ? color.trim() : "var(--brand-primary)";
  const badgeText = getBadgeTextColor(badgeColor);
  return { badgeColor, badgeText };
};

const listingTitle = categoryFilter
  ? `${categories.find((c) => c.slug === categoryFilter)?.label || "Category"} Recipes`
  : "All Recipes";
const siteTitle = searchTerm
  ? `${listingTitle} matching "${searchTerm}" - SaaS Blog`
  : `${listingTitle} - SaaS Blog`;
const siteDescription =
  "Browse all our delicious and easy-to-follow recipes. Filter by category or tag.";
---

<Layout title={siteTitle} description={siteDescription}>
  <main class="recipes-page">
    <!-- Page Header -->
    <section class="page-header">
      <div class="container">
        <h1 class="page-title">
          <span class="title-dot"></span>
          {
            listingTitle
          }
        </h1>
        <p class="recipe-count">
          {totalRecipes} recipes found{searchTerm ? ` for "${searchTerm}"` : ""}
        </p>
      </div>
    </section>

    <!-- Category Filter Pills -->
    <section class="filters-section">
      <div class="container">
        <div class="filter-pills">
          <a
            href={buildRecipeListingUrl({ search: searchTerm })}
            class:list={[
              "filter-pill",
              { active: !categoryFilter && !tagFilter },
            ]}
          >
            All
          </a>
          {
            categories.map((cat) => (
              <a
                href={buildRecipeListingUrl({
                  category: cat.slug === categoryFilter ? "" : cat.slug,
                  tag: tagFilter,
                  search: searchTerm,
                })}
                class:list={[
                  "filter-pill",
                  { active: categoryFilter === cat.slug },
                ]}
              >
                {cat.label}
              </a>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Tag Chips -->
    {
      tags.length > 0 && (
        <section class="tags-section">
          <div class="container">
            <div class="tag-chips">
              {tags.slice(0, 8).map((tag) => (
                <a
                  href={buildRecipeListingUrl({
                    category: categoryFilter,
                    tag: tag.slug === tagFilter ? "" : tag.slug,
                    search: searchTerm,
                  })}
                  class:list={["chip-standard tag-chip", { active: tagFilter === tag.slug }]}
                  style={`--tag-color: ${tag.color || "var(--brand-primary)"};`}
                >
                  <div class="chip-image">
                    {tag.image_url ? (
                      <img src={tag.image_url} alt={tag.label} />
                    ) : (
                      <div class="chip-placeholder">{tag.label.charAt(0)}</div>
                    )}
                  </div>
                  <span class="chip-label">{tag.label}</span>
                </a>
              ))}
            </div>
          </div>
        </section>
      )
    }

    <!-- Active Filters -->
    {
      (categoryFilter || tagFilter || searchTerm) && (
        <section class="active-filters">
          <div class="container">
            <div class="filter-tags">
              {categoryFilter && (
                <span class="filter-tag">
                  Category:{" "}
                  {categories.find((c) => c.slug === categoryFilter)?.label}
                  <a
                    href={buildRecipeListingUrl({
                      tag: tagFilter,
                      search: searchTerm,
                    })}
                    class="remove-filter"
                    aria-label="Remove category filter"
                  >
                    ×
                  </a>
                </span>
              )}
              {tagFilter && (
                <span class="filter-tag">
                  Tag: {tags.find((t) => t.slug === tagFilter)?.label}
                  <a
                    href={buildRecipeListingUrl({
                      category: categoryFilter,
                      search: searchTerm,
                    })}
                    class="remove-filter"
                    aria-label="Remove tag filter"
                  >
                    ×
                  </a>
                </span>
              )}
              {searchTerm && (
                <span class="filter-tag">
                  Search: "{searchTerm}"
                  <a
                    href={buildRecipeListingUrl({
                      category: categoryFilter,
                      tag: tagFilter,
                    })}
                    class="remove-filter"
                    aria-label="Clear search"
                  >
                    ×
                  </a>
                </span>
              )}
              <a href="/recipes" class="clear-all">
                Clear All
              </a>
            </div>
          </div>
        </section>
      )
    }

    <!-- Recipe Grid -->
    <section class="recipes-grid-section">
      <div class="container">
        <h2 class="sr-only">Recipe results</h2>
        {
          recipes.length > 0 ? (
            <div class="recipes-grid">
              {recipes.map((recipe) => {
                const { selected, srcSet } = getRecipeImage(recipe, 400, "hero");
                const sizes = "(max-width: 768px) 50vw, 25vw";
                const { badgeColor, badgeText } = getBadgeColors(recipe.category?.color);
                const cachedRecipe = parseCachedRecipe(recipe.cached_recipe_json);
                const prepTime = cachedRecipe?.prep_time_minutes ?? null;
                const servings = cachedRecipe?.servings ?? null;
                return (
                  <article class="recipe-card">
                    <button
                      type="button"
                      class="recipe-bookmark"
                      data-bookmark-button
                      data-bookmark-slug={recipe.slug}
                      aria-pressed="false"
                      aria-label="Bookmark recipe"
                    >
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 4a2 2 0 012-2h8a2 2 0 012 2v18l-6-4-6 4V4z" />
                      </svg>
                    </button>
                    <a href={`/recipes/${recipe.slug}`} class="card-image-link">
                      {(selected.image_url || recipe.image_url) && (
                        <img
                          src={selected.image_url || recipe.image_url}
                          alt={selected.imageAlt || recipe.imageAlt || recipe.headline}
                          width={selected.imageWidth || recipe.imageWidth || 400}
                          height={selected.imageHeight || recipe.imageHeight || 300}
                          srcset={srcSet || undefined}
                          sizes={srcSet ? sizes : undefined}
                          loading="lazy"
                          style={selected.imageStyle || undefined}
                        />
                      )}
                      <span
                        class="badge card-badge"
                        style={`background: ${badgeColor}; color: ${badgeText};`}
                      >
                        {recipe.category?.label}
                      </span>
                    </a>
                    <div class="card-body">
                      <h3 class="card-title">
                        <a href={`/recipes/${recipe.slug}`}>{recipe.headline}</a>
                      </h3>
                      <div class="card-meta">
                        {prepTime !== null && (
                          <span class="meta-item">
                            <svg
                              viewBox="0 0 24 24"
                              fill="none"
                              stroke="currentColor"
                              stroke-width="2"
                            >
                              <circle cx="12" cy="12" r="10" />
                              <path d="M12 6v6l4 2" />
                            </svg>
                            {`${prepTime} min`}
                          </span>
                        )}
                        {servings !== null && (
                          <span class="meta-item">
                            <svg
                              viewBox="0 0 24 24"
                              fill="none"
                              stroke="currentColor"
                              stroke-width="2"
                            >
                              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
                              <circle cx="9" cy="7" r="4" />
                            </svg>
                            {`${servings} servings`}
                          </span>
                        )}
                      </div>
                    </div>
                  </article>
                );
              })}
            </div>
          ) : (
            <div class="no-recipes">
              <p>No recipes found matching your filters.</p>
              <a href="/recipes" class="reset-btn">
                View All Recipes
              </a>
            </div>
          )
        }

        <!-- Pagination -->
        {
          totalPages > 1 && (
            <div style="margin-top: var(--space-12);">
              <Pagination 
                currentPage={page} 
                totalPages={totalPages} 
                baseUrl="/recipes" 
                preserveParams={{
                  ...(categoryFilter ? { category: categoryFilter } : {}),
                  ...(tagFilter ? { tag: tagFilter } : {}),
                  ...(searchTerm ? { search: searchTerm } : {}),
                }}
              />
            </div>
          )
        }
      </div>
    </section>
  </main>
</Layout>

<style>
  .recipes-page {
    background: var(--bg);
    min-height: 100vh;
  }

  .container {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 clamp(var(--space-4), 3vw, var(--space-8));
  }

  /* Page Header */
  .page-header {
    background: var(--bg-elevated);
    padding: var(--space-12) 0;
    border-bottom: 1px solid var(--border);
  }

  .page-title {
    font-size: clamp(var(--space-8), 4vw, var(--space-10));
    font-weight: var(--font-extrabold);
    color: var(--text);
    margin: 0 0 var(--space-2);
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .title-dot {
    width: var(--space-3);
    height: var(--space-3);
    background: var(--brand-primary);
    border-radius: var(--radius-full);
  }

  .recipe-count {
    color: var(--text-tertiary);
    font-size: var(--text-base);
    margin: 0;
  }

  /* Filter Pills */
  .filters-section {
    background: var(--bg-elevated);
    padding: var(--space-6) 0;
    border-bottom: 1px solid var(--border);
  }

  .filter-pills {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
  }

  .filter-pill {
    padding: var(--space-2) var(--space-5);
    border: 1px solid var(--border);
    border-radius: var(--radius-full);
    background: var(--bg-elevated);
    color: var(--text-secondary);
    text-decoration: none;
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    transition: all var(--transition-fast);
  }

  .filter-pill:hover,
  .filter-pill.active {
    background: var(--brand-primary);
    color: var(--text-inverse);
    border-color: var(--brand-primary);
  }

  /* Tag Chips */
  .tags-section {
    background: var(--bg-elevated);
    padding: var(--space-8) 0;
  }

  .tag-chips {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-8);
  }

  .tag-chip {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    text-decoration: none;
    transition: transform var(--transition-fast);
  }

  .tag-chip:hover {
    transform: translateY(-4px);
  }

  .chip-image {
    width: 70px;
    height: 70px;
    border-radius: var(--radius-full);
    overflow: hidden;
    border: 3px solid transparent;
    transition: border-color var(--transition-fast);
  }

  .tag-chip:hover .chip-image,
  .tag-chip.active .chip-image {
    border-color: var(--tag-color, var(--brand-primary));
  }

  .chip-placeholder {
    width: 100%;
    height: 100%;
    background: var(--tag-color, var(--brand-primary));
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-inverse);
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
  }

  .tag-chip.active .chip-label {
    color: var(--tag-color, var(--brand-primary));
  }

  /* Active Filters */
  .active-filters {
    padding: var(--space-4) 0;
    background: var(--bg);
  }

  .filter-tags {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
  }

  .filter-tag {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: 0.375rem var(--space-3);
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    font-size: var(--text-xs);
    color: var(--text-secondary);
  }

  .remove-filter {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    background: var(--border);
    border-radius: var(--radius-full);
    color: var(--text-tertiary);
    text-decoration: none;
    font-size: var(--text-base);
    line-height: 1;
  }

  .remove-filter:hover {
    background: var(--brand-primary);
    color: var(--text-inverse);
  }

  .clear-all {
    color: var(--brand-primary);
    font-size: var(--text-xs);
    font-weight: var(--font-medium);
    text-decoration: none;
  }

  .clear-all:hover {
    text-decoration: underline;
  }

  /* Recipe Grid */
  .recipes-grid-section {
    padding: var(--space-12) 0;
  }

  .recipes-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-8);
  }

  @media (max-width: 1024px) {
    .recipes-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .recipes-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-6);
    }
  }

  @media (max-width: 480px) {
    .recipes-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Recipe Card */
  .recipe-card {
    background: var(--bg-elevated);
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-sm);
    transition:
      transform var(--transition-slow),
      box-shadow var(--transition-slow);
    position: relative;
  }

  .recipe-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-hover);
  }

  .card-image-link {
    display: block;
    position: relative;
    aspect-ratio: 4/3;
    overflow: hidden;
  }

  .recipe-bookmark {
    position: absolute;
    top: 12px;
    right: 12px;
    width: 36px;
    height: 36px;
    border-radius: var(--radius-full);
    border: none;
    background: color-mix(in srgb, var(--text) 70%, transparent);
    color: var(--bg-elevated);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-sm);
    cursor: pointer;
    z-index: 2;
  }

  .recipe-bookmark svg {
    width: 18px;
    height: 18px;
  }

  .recipe-bookmark[data-bookmarked="true"] {
    background: var(--error, #ef4444);
    color: var(--text-inverse);
  }

  .card-image-link img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .recipe-card:hover .card-image-link img {
    transform: scale(1.05);
  }

  .card-badge {
    position: absolute;
    bottom: 12px;
    left: 12px;
  }

  .card-body {
    padding: var(--space-4);
  }

  .card-title {
    font-size: var(--text-base);
    font-weight: var(--font-bold);
    line-height: 1.4;
    margin: 0 0 var(--space-3);
    color: var(--text);
  }

  .card-title a {
    color: inherit;
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .card-title a:hover {
    color: var(--brand-primary);
  }

  .card-meta {
    display: flex;
    gap: var(--space-4);
  }

  .meta-item {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    font-size: var(--text-xs);
    color: var(--text-tertiary);
  }

  .meta-item svg {
    width: 14px;
    height: 14px;
  }

  /* No Recipes */
  .no-recipes {
    text-align: center;
    padding: var(--space-16) var(--space-8);
  }

  .no-recipes p {
    color: var(--text-tertiary);
    font-size: var(--text-lg);
    margin: 0 0 var(--space-6);
  }

  .reset-btn {
    display: inline-block;
    padding: var(--space-3) var(--space-6);
    background: var(--brand-primary);
    color: var(--text-inverse);
    text-decoration: none;
    border-radius: var(--radius-md);
    font-weight: var(--font-semibold);
    transition: background var(--transition-fast);
  }

  .reset-btn:hover {
    background: var(--brand-primary-hover);
  }
</style>
