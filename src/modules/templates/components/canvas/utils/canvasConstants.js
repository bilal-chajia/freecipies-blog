// Canvas Constants for PinCanvas
// Centralized configuration for Pinterest pin canvas

// Pin canvas dimensions (Pinterest standard 2:3 ratio)
export const CANVAS_WIDTH = 1000;
export const CANVAS_HEIGHT = 1500;

// Grid size for snapping (unified with useEditorStore)
export const GRID_SIZE = 20;

// Snap threshold for smart guides (in pixels, unified with useEditorStore)
export const SNAP_THRESHOLD = 8;

// Default zoom level
export const DEFAULT_ZOOM = 100;

// Zoom constraints
export const MIN_ZOOM = 25;
export const MAX_ZOOM = 200;
