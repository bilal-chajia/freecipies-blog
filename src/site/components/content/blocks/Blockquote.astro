---
import { renderInlineMarkdown } from '../../utils/markdown';
const { block } = Astro.props as { block: { type: 'blockquote'; text: string } };
---
<blockquote><p set:html={renderInlineMarkdown(block.text)} /></blockquote>
